AI_GATEWAY_TOKEN=
ATLASSIAN_SITE_URL=https://gjensidige.atlassian.net
# Live Atlassian search uses a separate, browser-approved local MCP login.
# Only enable the old snapshot pages when you have an authorized local evidence file.
# Only for a single-user research demo bound to 127.0.0.1, with a local ignored
# backend/private/evidence.json created from sources YOU are allowed to read.
LOCAL_RESEARCH_DEMO=false
# Slack integration, channel, trigger and any write action remain unconfigured.
